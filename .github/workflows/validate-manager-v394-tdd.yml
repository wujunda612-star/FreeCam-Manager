name: Validate Manager V3.9.4 TDD

on:
  push:
    branches:
      - feature/manager-v3.9.4-startup-diagnostics
    paths:
      - .github/workflows/validate-manager-v394-tdd.yml
      - manager/incoming/v3.9.4/test_v394_startup_diagnostics.py
      - manager/incoming/v3.9.4/apply_v394.py

jobs:
  contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build V3.9.4 candidate from V3.9.3 baseline
        shell: python
        run: |
          import shutil, subprocess, sys, zipfile
          from pathlib import Path
          src = Path('manager/packages/FreeCam_Manager_V3.9.3_Source.zip')
          root = Path('manager/incoming/v3.9.4/_candidate')
          if root.exists(): shutil.rmtree(root)
          root.mkdir(parents=True)
          with zipfile.ZipFile(src) as zf: zf.extractall(root)
          subprocess.run([sys.executable, 'manager/incoming/v3.9.4/apply_v394.py', str(root)], check=True)
          out = Path('manager/incoming/v3.9.4/FreeCam_Manager_V3.9.4_Source.zip')
          if out.exists(): out.unlink()
          with zipfile.ZipFile(out, 'w', zipfile.ZIP_DEFLATED) as zf:
              for p in sorted(root.rglob('*')):
                  if p.is_file(): zf.write(p, p.relative_to(root).as_posix())
      - name: Run V3.9.4 diagnostics contract (GREEN)
        run: python manager/incoming/v3.9.4/test_v394_startup_diagnostics.py manager/incoming/v3.9.4/FreeCam_Manager_V3.9.4_Source.zip
